import { siteInfo } from "@/data/site";

export const metadata = {
  title: "Profil",
  description:
    "Profil resmi Kementerian Agama Kabupaten Barito Utara, mencakup sejarah, visi misi, tugas fungsi, dan tujuan kelembagaan.",
};

export default function ProfilLayout({ children }) {
  return children;
}
